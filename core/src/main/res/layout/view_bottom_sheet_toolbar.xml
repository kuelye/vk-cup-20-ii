<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:minHeight="@dimen/bottom_sheet_toolbar_height"
    tools:parentTag="android.widget.RelativeLayout">

    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="@dimen/padding_standard"
        android:fontFamily="sans-serif-medium"
        android:textColor="@color/title_color"
        android:textSize="16sp"
        tools:text="Title" />

    <ImageView
        android:id="@+id/dismissImageView"
        android:layout_width="@dimen/small_icon_size_24"
        android:layout_height="@dimen/small_icon_size_24"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@drawable/bg_circle_button"
        android:layout_marginEnd="@dimen/padding_standard"
        android:src="@drawable/ic_dismiss_24"
        android:tint="@color/icon_color" />
</merge>